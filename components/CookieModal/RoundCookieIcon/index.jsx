import React from 'react';


const RoundCookieIcon = ({ onClick }) => {
  return (
    <div className="roundCookieIcon" onClick={onClick}>
      🍪
    </div>
  );
};

export default RoundCookieIcon;
